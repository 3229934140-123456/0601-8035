# 可删除布隆过滤器技术分析

## 一、计数布隆过滤器（Counting Bloom Filter）

### 1.1 基本原理

标准布隆过滤器使用比特位数组，每个位置只有 0/1 两种状态。计数布隆过滤器将每个比特位替换为一个**计数器**（通常是 4 位或 8 位），从而支持删除操作：

- **插入**：k 个哈希函数定位到 k 个计数器，每个计数器 +1
- **查询**：检查 k 个计数器是否都 > 0
- **删除**：k 个计数器分别 -1（前提是元素存在）

代码实现在 [counting_bloom_filter.py](file:///d:/trae-bz/TraeProjects/8035/counting_bloom_filter.py) 中。

### 1.2 计数器溢出问题

#### 问题本质

计数器位数有限（常见 4 位 = 15，8 位 = 255），当同一个位置被大量元素的哈希映射到时，计数器会达到最大值后**无法继续递增**，这就是溢出。

溢出带来的后果：

1. **删除操作失效**：如果计数器已溢出（饱和），删除元素时计数器不会减少。即使删除了很多元素，计数器仍然停留在最大值，导致：
   - 已删除的元素仍然被误判为存在（假阳性率上升）
   - 过滤器状态"损坏"，无法正确反映真实的元素集合

2. **假阳性率上升**：溢出意味着计数器失去了计数能力，退化为标准布隆过滤器的比特位，但删除操作无法正确回退。

3. **不可逆损坏**：一旦发生溢出，即使后续删除元素，计数器也不会下降，过滤器的准确性会持续下降。

#### 溢出概率分析

对于 n 个元素、m 个计数器、k 个哈希函数，单个计数器被增加的期望次数为：

```
E[X] = n * k / m
```

当 `n*k/m` 接近计数器最大值时，溢出风险显著增加。

#### 应对策略

1. **增加计数器位数**：从 4 位增加到 8 位，但空间开销翻倍
2. **定期重建**：当溢出率超过阈值时，用真实元素集合重建过滤器
3. **保守删除**：删除前严格检查元素是否存在，减少误删
4. **增量式扩展**：动态增加计数器数组大小（实现复杂）

代码中 `overflow_risk()` 方法用于监控溢出风险：

```python
def overflow_risk(self) -> float:
    overflowed = sum(1 for c in self.counters if c >= self.max_counter)
    return overflowed / self.num_counters
```

测试中可以观察到溢出现象（2位计数器，最大值3）：

```
After 10 inserts of same item, contains: True
After 1 delete, contains: True  ← 溢出导致删除失效
```

### 1.3 空间开销

标准布隆过滤器每个元素约需要 `1.44 * log2(1/ε)` 位空间。计数布隆过滤器由于每个位置需要 c 位计数器，空间扩大 c 倍：

- 4位计数器：每个元素约 `5.76 * log2(1/ε)` 位
- 8位计数器：每个元素约 `11.52 * log2(1/ε)` 位

以 1% 假阳性率为例：
- 标准布隆：~9.6 位/元素
- 4位计数布隆：~38.3 位/元素（约4倍）
- 8位计数布隆：~76.8 位/元素（约8倍）

---

## 二、Cuckoo 过滤器（Cuckoo Filter）

### 2.1 基本原理

Cuckoo 过滤器基于 Cuckoo 哈希（Cuckoo Hashing）思想，使用**部分指纹**（fingerprint）存储元素，每个元素有两个候选桶（bucket）。

核心机制：

1. **指纹（Fingerprint）**：对元素哈希得到 f 位指纹，用于在桶中标识元素
2. **两个候选位置**：
   - i1 = hash(item)
   - i2 = i1 ^ hash(fingerprint)
   （利用异或的对称性：`i1 ^ h = i2` → `i2 ^ h = i1`）
3. **Cuckoo 踢出**：如果两个桶都满了，随机踢出一个桶中的元素，将其放到它的另一个候选桶中，重复此过程直到找到空位或达到最大踢出次数

代码实现在 [cuckoo_filter.py](file:///d:/trae-bz/TraeProjects/8035/cuckoo_filter.py) 中。

### 2.2 为什么需要删除且追求空间效率时 Cuckoo Filter 更优？

#### 原因一：删除是"真删除"，没有副作用

计数布隆过滤器的删除是**递减计数器**，但：
- 可能因溢出导致删除失效
- 假阳性元素也能被"删除"（计数器被错误递减）
- 删除操作本身可能引入假阴性（如果误删了不存在的元素）

Cuckoo 过滤器的删除是**直接移除指纹**：
- 找到指纹所在的桶位置，设为空即可
- 删除后元素确实不存在了，不会有残留
- 不会影响其他元素的正确性

测试验证：
```
Cuckoo: Deleted items still present (false pos): 0/100
Counting Bloom: 删除后仍可能因溢出存在残留
```

#### 原因二：空间效率更高

Cuckoo 过滤器每个元素只需要存储一个 f 位的指纹 + 桶管理开销。

空间对比（理论值，ε ≈ 1%）：

| 过滤器类型 | 空间/元素 | 说明 |
|-----------|----------|------|
| 标准布隆 | ~9.6 bits | 不支持删除 |
| 4位计数布隆 | ~38.3 bits | 支持删除，有溢出风险 |
| Cuckoo (12位指纹) | ~12-14 bits | 支持删除，高负载因子 |
| Cuckoo (16位指纹) | ~16-18 bits | 更低的假阳性率 |

我们的实测数据：
```
Memory per item (bits):
  Counting Bloom: 38.34
  Cuckoo:         19.66
```

Cuckoo 空间效率约为 4位计数布隆的 **2 倍**。

空间效率高的原因：
1. **指纹存储**：只存 f 位指纹，而不是 k 个计数器
2. **高负载因子**：Cuckoo 哈希可以达到 ~95% 的负载因子，而无需预留太多空闲空间
3. **删除不浪费空间**：删除后槽位可以立即被复用

#### 原因三：假阳性率更可控

计数布隆过滤器随着插入元素增多，假阳性率单调上升。而 Cuckoo 过滤器的假阳性率主要由指纹位数决定：

```
ε ≈ 2 * b * 2^(-f) / 2
```

其中 b 是桶大小，f 是指纹位数。调整指纹位数可以精确控制假阳性率。

---

## 三、Cuckoo Filter 的代价：查找与插入失败处理

虽然 Cuckoo 过滤器在删除和空间效率上有优势，但也付出了相应的代价。

### 3.1 查找代价

#### 计数布隆过滤器的查找

- 需要计算 k 个哈希值
- 访问 k 个计数器（随机内存访问）
- 逻辑简单：全部 > 0 则存在
- 时间复杂度：O(k)，k 通常为 4-7

#### Cuckoo 过滤器的查找

- 需要计算 2 个哈希位置（i1 和 i2）
- 每个桶内需要线性扫描 b 个槽位（b 通常为 2-8）
- 指纹比较（而不仅仅是检查 0/1）
- 时间复杂度：O(b)，需要比较最多 2b 个指纹

实测性能：
```
Query time (s) for 5000 queries:
  Counting Bloom: 0.0374
  Cuckoo:         0.0487
```

Cuckoo 查询稍慢，因为：
1. 需要比较指纹内容（整数比较）
2. 桶内需要顺序扫描多个槽位
3. 缓存局部性略差（两个桶可能不相邻）

### 3.2 插入失败处理

#### 计数布隆过滤器的插入

- **永不失败**：只要空间足够，插入总会成功
- 只是假阳性率会上升，但不会拒绝插入
- 实现简单，无需复杂逻辑

#### Cuckoo 过滤器的插入

- **可能失败**：当过滤器接近满负载时，Cuckoo 踢出可能无法在 max_kicks 步内找到空位
- 插入失败需要调用方处理（扩容、拒绝等）
- 插入性能不稳定：大部分时候很快（O(1)），但偶尔需要多次踢出

测试中观察到的高负载插入：
```
Insertion failed at item 247, load factor: 0.9648
```

插入失败的代价：
1. **实现复杂度高**：需要循环踢出逻辑、随机选择、边界处理
2. **性能抖动**：平均 O(1)，最坏情况 O(max_kicks)
3. **容量规划难**：需要预留一定空闲空间来保证插入成功率
4. **需要扩容机制**：插入失败时可能需要扩容，实现复杂

### 3.3 其他代价

| 方面 | 计数布隆 | Cuckoo |
|-----|---------|--------|
| 实现难度 | 简单 | 中等 |
| 插入确定性 | 确定成功 | 可能失败 |
| 插入性能稳定性 | 稳定 | 有抖动 |
| 并发性 | 易实现（每个计数器独立锁） | 较难（踢出涉及多个桶） |
| 序列化 | 简单 | 稍复杂 |

---

## 四、总结与选型建议

### 核心对比

| 特性 | 计数布隆过滤器 | Cuckoo 过滤器 |
|-----|--------------|--------------|
| 空间效率 | 低（~38 bits/元素 @ 1% FP） | 高（~13 bits/元素 @ 1% FP） |
| 删除支持 | 支持，但有溢出风险 | 支持，真删除 |
| 删除正确性 | 可能因溢出失效 | 正确可靠 |
| 插入 | 永不失败 | 高负载时可能失败 |
| 查找速度 | 快（k次内存访问） | 稍慢（2b次比较） |
| 实现复杂度 | 低 | 中 |
| 假阳性率控制 | 随元素数增加 | 由指纹位数决定 |

### 选型建议

**选择计数布隆过滤器当：**
- 删除操作不频繁
- 元素数量相对稳定，不会大幅超过预期
- 实现简单性优先
- 插入不能失败（必须成功）
- 可以接受较高的空间开销

**选择 Cuckoo 过滤器当：**
- 需要频繁的删除操作
- 空间效率很重要（内存受限场景）
- 删除后需要准确的结果（不能有残留）
- 可以接受偶尔的插入失败或有扩容机制
- 追求接近最优的空间-准确率权衡

### 代码位置

- 计数布隆过滤器实现：[counting_bloom_filter.py](file:///d:/trae-bz/TraeProjects/8035/counting_bloom_filter.py)
- Cuckoo 过滤器实现：[cuckoo_filter.py](file:///d:/trae-bz/TraeProjects/8035/cuckoo_filter.py)
- 测试与基准脚本：[test_filters.py](file:///d:/trae-bz/TraeProjects/8035/test_filters.py)
